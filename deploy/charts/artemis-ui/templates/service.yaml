apiVersion: v1
kind: Service
metadata:
  name: {{ include "artemis-ui.fullname" . }}
  labels:
    {{- include "artemis-ui.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "artemis-ui.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
